version: "3"

vars:
  TASKS: '{{.TASKS | default "mlvu_dev"}}'
  SMOKE_TASKS: '{{.SMOKE_TASKS | default "mlvu_dev"}}'
  SMOKE_LIMIT: '{{.SMOKE_LIMIT | default "1"}}'
  FRAMES: '{{.FRAMES | default "48"}}'
  MIN_PIXELS: '{{.MIN_PIXELS | default "200704"}}'
  MAX_PIXELS: '{{.MAX_PIXELS | default "401408"}}'
  FPS: '{{.FPS | default ""}}'
  LOG_VIDEO_GRID: '{{.LOG_VIDEO_GRID | default "0"}}'
  VIDEO_READER: '{{.VIDEO_READER | default "torchcodec"}}'
  STRICT_VIDEO_READER: '{{.STRICT_VIDEO_READER | default "1"}}'
  TORCHCODEC_NUM_THREADS: '{{.TORCHCODEC_NUM_THREADS | default "8"}}'
  ATTN_IMPLEMENTATION: '{{.ATTN_IMPLEMENTATION | default "flash_attention_2"}}'
  GPUS: '{{.GPUS | default "1"}}'
  PORT: '{{.PORT | default "12346"}}'
  OUTPUT_PATH: '{{.OUTPUT_PATH | default "./results"}}'
  WANDB_ENTITY: '{{.WANDB_ENTITY | default "dlehddnjs245-kyung-hee-university"}}'
  WANDB_PROJECT: '{{.WANDB_PROJECT | default "mllm-quant"}}'
  # On by default: without per-sample logs there is no way to tell a cache that changed the
  # generations from one that silently did nothing. Set LOG_SAMPLES=0 to opt out.
  LOG_SAMPLES: '{{.LOG_SAMPLES | default "1"}}'
  CUDA_VISIBLE_DEVICES: '{{.CUDA_VISIBLE_DEVICES | default "0"}}'
  HF_HOME: '{{.HF_HOME | default "$HOME/.cache/huggingface"}}'
  KIVI_OUTPUT_PATH: '{{.KIVI_OUTPUT_PATH | default "./results/kivi-cache-experiment.json"}}'
  KIVI_EVAL_OUTPUT_PATH: '{{.KIVI_EVAL_OUTPUT_PATH | default "./results/kivi-lmms-eval"}}'
  KIVI_DEVICE: '{{.KIVI_DEVICE | default "auto"}}'
  KIVI_DTYPE: '{{.KIVI_DTYPE | default "float32"}}'
  KIVI_SEED: '{{.KIVI_SEED | default "0"}}'
  KIVI_BATCH_SIZE: '{{.KIVI_BATCH_SIZE | default "1"}}'
  KIVI_KV_HEADS: '{{.KIVI_KV_HEADS | default "8"}}'
  KIVI_HEAD_DIM: '{{.KIVI_HEAD_DIM | default "128"}}'
  KIVI_LENGTHS: '{{.KIVI_LENGTHS | default "200,56,1"}}'
  KIVI_NBITS: '{{.KIVI_NBITS | default "2"}}'
  KIVI_Q_GROUP_SIZE: '{{.KIVI_Q_GROUP_SIZE | default "32"}}'
  KIVI_RESIDUAL_LENGTH: '{{.KIVI_RESIDUAL_LENGTH | default "128"}}'
  VIDKV_OUTPUT_PATH: '{{.VIDKV_OUTPUT_PATH | default "./results/vidkv-cache-experiment.json"}}'
  VIDKV_EVAL_OUTPUT_PATH: '{{.VIDKV_EVAL_OUTPUT_PATH | default "./results/vidkv-lmms-eval"}}'
  VIDKV_NBITS_KEY: '{{.VIDKV_NBITS_KEY | default "1.5"}}'
  VIDKV_NBITS_VALUE: '{{.VIDKV_NBITS_VALUE | default "1.58"}}'
  VIDKV_TERNARY_THRESHOLD: '{{.VIDKV_TERNARY_THRESHOLD | default "0.7"}}'
  VIDKV_Q_GROUP_SIZE: '{{.VIDKV_Q_GROUP_SIZE | default "32"}}'
  VIDKV_RESIDUAL_LENGTH: '{{.VIDKV_RESIDUAL_LENGTH | default "128"}}'

tasks:
  setup:
    desc: Set up the Pixi environment and verify CUDA/FlashAttention2.
    cmds:
      - bash scripts/setup_pixi.sh

  setup-venv:
    desc: Set up the legacy uv virtualenv and verify CUDA.
    cmds:
      - ./scripts/setup_env.sh

  smoke:
    desc: Run a one-sample Qwen2.5-VL lmms-eval smoke test.
    aliases: [smote]
    cmds:
      - >-
        CUDA_VISIBLE_DEVICES={{.CUDA_VISIBLE_DEVICES}} HF_HOME={{.HF_HOME}} PYTHONNOUSERSITE=1 PYTHONPATH= LOG_VIDEO_GRID={{.LOG_VIDEO_GRID}}
        FORCE_QWENVL_VIDEO_READER={{.VIDEO_READER}} STRICT_VIDEO_READER={{.STRICT_VIDEO_READER}} TORCHCODEC_NUM_THREADS={{.TORCHCODEC_NUM_THREADS}}
        pixi run accelerate launch
        --num_processes={{.GPUS}}
        --main_process_port={{.PORT}}
        -m lmms_eval
        --model qwen2_5_vl
        --model_args pretrained=Qwen/Qwen2.5-VL-7B-Instruct,min_pixels={{.MIN_PIXELS}},max_pixels={{.MAX_PIXELS}},attn_implementation={{.ATTN_IMPLEMENTATION}},interleave_visuals=False,max_num_frames={{.FRAMES}}{{if .FPS}},fps={{.FPS}}{{end}}
        --tasks {{.SMOKE_TASKS}}
        --batch_size 1
        --limit {{.SMOKE_LIMIT}}
        --output_path {{.OUTPUT_PATH}}
        {{if eq .LOG_SAMPLES "1"}}--log_samples{{end}}
        {{if .wandb}}--wandb_args 'entity={{.WANDB_ENTITY}},project={{.WANDB_PROJECT}},name={{.wandb}}'{{else if .WANDB}}--wandb_args 'entity={{.WANDB_ENTITY}},project={{.WANDB_PROJECT}},name={{.WANDB}}'{{end}}

  smoke-kivi:
    desc: Run a one-sample Qwen2.5-VL lmms-eval smoke test with KIVI cache.
    cmds:
      - >-
        CUDA_VISIBLE_DEVICES={{.CUDA_VISIBLE_DEVICES}} HF_HOME={{.HF_HOME}} PYTHONNOUSERSITE=1 PYTHONPATH= LOG_VIDEO_GRID={{.LOG_VIDEO_GRID}}
        FORCE_QWENVL_VIDEO_READER={{.VIDEO_READER}} STRICT_VIDEO_READER={{.STRICT_VIDEO_READER}} TORCHCODEC_NUM_THREADS={{.TORCHCODEC_NUM_THREADS}}
        pixi run accelerate launch
        --num_processes={{.GPUS}}
        --main_process_port={{.PORT}}
        -m lmms_eval
        --model qwen2_5_vl
        --model_args pretrained=Qwen/Qwen2.5-VL-7B-Instruct,min_pixels={{.MIN_PIXELS}},max_pixels={{.MAX_PIXELS}},attn_implementation={{.ATTN_IMPLEMENTATION}},interleave_visuals=False,max_num_frames={{.FRAMES}}{{if .FPS}},fps={{.FPS}}{{end}},kv_cache=kivi,kivi_nbits={{.KIVI_NBITS}},kivi_q_group_size={{.KIVI_Q_GROUP_SIZE}},kivi_residual_length={{.KIVI_RESIDUAL_LENGTH}}
        --tasks {{.SMOKE_TASKS}}
        --batch_size 1
        --limit {{.SMOKE_LIMIT}}
        --output_path {{.KIVI_EVAL_OUTPUT_PATH}}
        {{if eq .LOG_SAMPLES "1"}}--log_samples{{end}}
        {{if .wandb}}--wandb_args 'entity={{.WANDB_ENTITY}},project={{.WANDB_PROJECT}},name={{.wandb}}'{{else if .WANDB}}--wandb_args 'entity={{.WANDB_ENTITY}},project={{.WANDB_PROJECT}},name={{.WANDB}}'{{end}}

  smoke-vidkv:
    desc: Run a one-sample Qwen2.5-VL lmms-eval smoke test with VidKV cache.
    cmds:
      - >-
        CUDA_VISIBLE_DEVICES={{.CUDA_VISIBLE_DEVICES}} HF_HOME={{.HF_HOME}} PYTHONNOUSERSITE=1 PYTHONPATH= LOG_VIDEO_GRID={{.LOG_VIDEO_GRID}}
        FORCE_QWENVL_VIDEO_READER={{.VIDEO_READER}} STRICT_VIDEO_READER={{.STRICT_VIDEO_READER}} TORCHCODEC_NUM_THREADS={{.TORCHCODEC_NUM_THREADS}}
        pixi run accelerate launch
        --num_processes={{.GPUS}}
        --main_process_port={{.PORT}}
        -m lmms_eval
        --model qwen2_5_vl
        --model_args pretrained=Qwen/Qwen2.5-VL-7B-Instruct,min_pixels={{.MIN_PIXELS}},max_pixels={{.MAX_PIXELS}},attn_implementation={{.ATTN_IMPLEMENTATION}},interleave_visuals=False,max_num_frames={{.FRAMES}}{{if .FPS}},fps={{.FPS}}{{end}},kv_cache=vidkv,vidkv_nbits_key={{.VIDKV_NBITS_KEY}},vidkv_nbits_value={{.VIDKV_NBITS_VALUE}},vidkv_ternary_threshold={{.VIDKV_TERNARY_THRESHOLD}},vidkv_q_group_size={{.VIDKV_Q_GROUP_SIZE}},vidkv_residual_length={{.VIDKV_RESIDUAL_LENGTH}}
        --tasks {{.SMOKE_TASKS}}
        --batch_size 1
        --limit {{.SMOKE_LIMIT}}
        --output_path {{.VIDKV_EVAL_OUTPUT_PATH}}
        {{if eq .LOG_SAMPLES "1"}}--log_samples{{end}}
        {{if .wandb}}--wandb_args 'entity={{.WANDB_ENTITY}},project={{.WANDB_PROJECT}},name={{.wandb}}'{{else if .WANDB}}--wandb_args 'entity={{.WANDB_ENTITY}},project={{.WANDB_PROJECT}},name={{.WANDB}}'{{end}}

  full:
    desc: Run the full Qwen2.5-VL lmms-eval experiment.
    vars:
      RUN_STAMP:
        sh: date +%Y%m%d-%H%M%S
      FULL_WANDB_RUN: '{{.wandb | default .WANDB | default (printf "full-%s" .RUN_STAMP)}}'
    cmds:
      - >-
        CUDA_VISIBLE_DEVICES={{.CUDA_VISIBLE_DEVICES}} HF_HOME={{.HF_HOME}} PYTHONNOUSERSITE=1 PYTHONPATH= LOG_VIDEO_GRID={{.LOG_VIDEO_GRID}}
        FORCE_QWENVL_VIDEO_READER={{.VIDEO_READER}} STRICT_VIDEO_READER={{.STRICT_VIDEO_READER}} TORCHCODEC_NUM_THREADS={{.TORCHCODEC_NUM_THREADS}}
        pixi run accelerate launch
        --num_processes={{.GPUS}}
        --main_process_port={{.PORT}}
        -m lmms_eval
        --model qwen2_5_vl
        --model_args pretrained=Qwen/Qwen2.5-VL-7B-Instruct,min_pixels={{.MIN_PIXELS}},max_pixels={{.MAX_PIXELS}},attn_implementation={{.ATTN_IMPLEMENTATION}},interleave_visuals=False,max_num_frames={{.FRAMES}}{{if .FPS}},fps={{.FPS}}{{end}}
        --tasks {{.TASKS}}
        --batch_size 1
        --output_path {{.OUTPUT_PATH}}
        {{if eq .LOG_SAMPLES "1"}}--log_samples{{end}}
        --wandb_args 'entity={{.WANDB_ENTITY}},project={{.WANDB_PROJECT}},name={{.FULL_WANDB_RUN}}'

  full-kivi:
    desc: Run the full Qwen2.5-VL lmms-eval experiment with KIVI cache.
    cmds:
      - >-
        CUDA_VISIBLE_DEVICES={{.CUDA_VISIBLE_DEVICES}} HF_HOME={{.HF_HOME}} PYTHONNOUSERSITE=1 PYTHONPATH= LOG_VIDEO_GRID={{.LOG_VIDEO_GRID}}
        FORCE_QWENVL_VIDEO_READER={{.VIDEO_READER}} STRICT_VIDEO_READER={{.STRICT_VIDEO_READER}} TORCHCODEC_NUM_THREADS={{.TORCHCODEC_NUM_THREADS}}
        pixi run accelerate launch
        --num_processes={{.GPUS}}
        --main_process_port={{.PORT}}
        -m lmms_eval
        --model qwen2_5_vl
        --model_args pretrained=Qwen/Qwen2.5-VL-7B-Instruct,min_pixels={{.MIN_PIXELS}},max_pixels={{.MAX_PIXELS}},attn_implementation={{.ATTN_IMPLEMENTATION}},interleave_visuals=False,max_num_frames={{.FRAMES}}{{if .FPS}},fps={{.FPS}}{{end}},kv_cache=kivi,kivi_nbits={{.KIVI_NBITS}},kivi_q_group_size={{.KIVI_Q_GROUP_SIZE}},kivi_residual_length={{.KIVI_RESIDUAL_LENGTH}}
        --tasks {{.TASKS}}
        --batch_size 1
        --output_path {{.KIVI_EVAL_OUTPUT_PATH}}
        {{if eq .LOG_SAMPLES "1"}}--log_samples{{end}}
        {{if .wandb}}--wandb_args 'entity={{.WANDB_ENTITY}},project={{.WANDB_PROJECT}},name={{.wandb}}'{{else if .WANDB}}--wandb_args 'entity={{.WANDB_ENTITY}},project={{.WANDB_PROJECT}},name={{.WANDB}}'{{end}}

  full-vidkv:
    desc: Run the full Qwen2.5-VL lmms-eval experiment with VidKV cache.
    cmds:
      - >-
        CUDA_VISIBLE_DEVICES={{.CUDA_VISIBLE_DEVICES}} HF_HOME={{.HF_HOME}} PYTHONNOUSERSITE=1 PYTHONPATH= LOG_VIDEO_GRID={{.LOG_VIDEO_GRID}}
        FORCE_QWENVL_VIDEO_READER={{.VIDEO_READER}} STRICT_VIDEO_READER={{.STRICT_VIDEO_READER}} TORCHCODEC_NUM_THREADS={{.TORCHCODEC_NUM_THREADS}}
        pixi run accelerate launch
        --num_processes={{.GPUS}}
        --main_process_port={{.PORT}}
        -m lmms_eval
        --model qwen2_5_vl
        --model_args pretrained=Qwen/Qwen2.5-VL-7B-Instruct,min_pixels={{.MIN_PIXELS}},max_pixels={{.MAX_PIXELS}},attn_implementation={{.ATTN_IMPLEMENTATION}},interleave_visuals=False,max_num_frames={{.FRAMES}}{{if .FPS}},fps={{.FPS}}{{end}},kv_cache=vidkv,vidkv_nbits_key={{.VIDKV_NBITS_KEY}},vidkv_nbits_value={{.VIDKV_NBITS_VALUE}},vidkv_ternary_threshold={{.VIDKV_TERNARY_THRESHOLD}},vidkv_q_group_size={{.VIDKV_Q_GROUP_SIZE}},vidkv_residual_length={{.VIDKV_RESIDUAL_LENGTH}}
        --tasks {{.TASKS}}
        --batch_size 1
        --output_path {{.VIDKV_EVAL_OUTPUT_PATH}}
        {{if eq .LOG_SAMPLES "1"}}--log_samples{{end}}
        {{if .wandb}}--wandb_args 'entity={{.WANDB_ENTITY}},project={{.WANDB_PROJECT}},name={{.wandb}}'{{else if .WANDB}}--wandb_args 'entity={{.WANDB_ENTITY}},project={{.WANDB_PROJECT}},name={{.WANDB}}'{{end}}

  run:
    desc: Run the full FP16, KIVI, and VidKV lmms-eval experiments sequentially.
    vars:
      RUN_STAMP:
        sh: date +%Y%m%d-%H%M%S
      RUN_NAME: '{{.wandb | default .WANDB | default (printf "run-%s" .RUN_STAMP)}}'
    cmds:
      - task: full
        vars:
          wandb: ""
          WANDB: '{{.RUN_NAME}}-fp16'
      - task: full-kivi
        vars:
          wandb: ""
          WANDB: '{{.RUN_NAME}}-kivi'
      - task: full-vidkv
        vars:
          wandb: ""
          WANDB: '{{.RUN_NAME}}-vidkv'

  quantize:
    desc: Run the full KIVI and VidKV lmms-eval experiments back to back, both logged to W&B.
    vars:
      RUN_STAMP:
        sh: date +%Y%m%d-%H%M%S
      # W&B is on by default here: these are multi-hour runs and losing one to a forgotten
      # flag is expensive. `wandb=`/`WANDB=` names the pair; otherwise fall back to a stamp.
      QUANTIZE_RUN: '{{.wandb | default .WANDB | default (printf "quantize-%s" .RUN_STAMP)}}'
    cmds:
      # Each leg gets its own W&B run name, otherwise both land under one name and the two
      # caches are indistinguishable in the dashboard. `wandb: ""` clears the lowercase var
      # so the per-leg WANDB below wins.
      - task: full-kivi
        vars:
          wandb: ""
          WANDB: '{{.QUANTIZE_RUN}}-kivi'
      - task: full-vidkv
        vars:
          wandb: ""
          WANDB: '{{.QUANTIZE_RUN}}-vidkv'

  kivi-cache-experiment:
    desc: Run standalone KIVI cache correctness, split, and storage experiment.
    cmds:
      - >-
        PYTHONNOUSERSITE=1 PYTHONPATH=
        pixi run python -m py_compile
        patches/kivi_cache.py
        scripts/run_kivi_cache_experiment.py
      - >-
        PYTHONNOUSERSITE=1 PYTHONPATH=
        pixi run python scripts/run_kivi_cache_experiment.py
        --output-path {{.KIVI_OUTPUT_PATH}}
        --seed {{.KIVI_SEED}}
        --device {{.KIVI_DEVICE}}
        --dtype {{.KIVI_DTYPE}}
        --batch-size {{.KIVI_BATCH_SIZE}}
        --kv-heads {{.KIVI_KV_HEADS}}
        --head-dim {{.KIVI_HEAD_DIM}}
        --lengths {{.KIVI_LENGTHS}}
        --nbits {{.KIVI_NBITS}}
        --q-group-size {{.KIVI_Q_GROUP_SIZE}}
        --residual-length {{.KIVI_RESIDUAL_LENGTH}}

  vidkv-cache-experiment:
    desc: Run standalone VidKV cache correctness, bit-budget, and error-drift experiment.
    cmds:
      - >-
        PYTHONNOUSERSITE=1 PYTHONPATH=
        pixi run python -m py_compile
        patches/vidkv_cache.py
        scripts/run_vidkv_cache_experiment.py
      - >-
        PYTHONNOUSERSITE=1 PYTHONPATH=
        pixi run python scripts/run_vidkv_cache_experiment.py
        --output-path {{.VIDKV_OUTPUT_PATH}}
        --seed {{.KIVI_SEED}}
        --device {{.KIVI_DEVICE}}
        --dtype {{.KIVI_DTYPE}}
        --batch-size {{.KIVI_BATCH_SIZE}}
        --head-dim {{.KIVI_HEAD_DIM}}
        --nbits-key {{.VIDKV_NBITS_KEY}}
        --nbits-value {{.VIDKV_NBITS_VALUE}}
        --ternary-threshold {{.VIDKV_TERNARY_THRESHOLD}}
        --q-group-size {{.VIDKV_Q_GROUP_SIZE}}
        --residual-length {{.VIDKV_RESIDUAL_LENGTH}}
